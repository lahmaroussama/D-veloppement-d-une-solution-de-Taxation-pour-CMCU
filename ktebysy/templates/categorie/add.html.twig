{% extends 'base.html.twig' %}
{% block body %}

<div class="card">
            <div class="card-body">
              <h5 class="card-title">Ajouter Categorie</h5>

              <!-- General Form Elements -->
              
                
               {{ form_start(f,{attr:{novalidate:'novalidate'}}) }}
                        <label for="inputText" class="col-sm-4 col-form-label">Nom</label>
                        <div class="col-sm-10">
                            {{ form_widget(f.nom_categorie,{'attr':{'class':'form-control'}}) }}
                            {{ form_errors(f.nom_categorie,{'attr':{'class':'text-danger'}}) }}
                        </div>
                    
               
                
               </br>

                <div class="col-sm-10" style="display: flex;align-items: center;justify-content: center">

                        {{ form_widget(f.ajouter,{'attr':{'class':'btn btn-primary'}}) }}
                    </div>
                      <form>
                        <input type = "button" value = "Retour!"  onclick = "history.back()">
                       </form>
           {{ form_end(f) }}

            </div>
          </div>
{% endblock %}